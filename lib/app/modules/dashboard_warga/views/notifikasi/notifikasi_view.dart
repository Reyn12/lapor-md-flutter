import 'package:flutter/widgets.dart';

class NotifikasiView extends StatelessWidget {
  const NotifikasiView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Ini Tampilan Notifikasi Dashboard Warga'),
    );
  }
}